import { style } from "@vanilla-extract/css";

export const page = style({
  maxWidth: "40rem",
  margin: "auto",
});

export const details = style({
  display: "flex",
  gap: "1rem",
  alignItems: "baseline",
});
